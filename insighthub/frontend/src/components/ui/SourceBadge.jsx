function SourceBadge({ name }) {
  return (
    <span className="inline-flex rounded-md border border-slate-700 bg-slate-800/80 px-2 py-0.5 text-xs text-slate-300">
      {name}
    </span>
  );
}

export default SourceBadge;
